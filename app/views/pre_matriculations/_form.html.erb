<%= form_for @pre_matriculation, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :person_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :person_id, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :curse_type_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :curse_type_id, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :languaje_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :languaje_id, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :state, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :state, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                pre_matriculations_path, :class => 'btn' %>
  </div>
<% end %>
