<%= form_for @document, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :matriculation_validate_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :matriculation_validate_id, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :document_type_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :document_type_id, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :document_code, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :document_code, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                documents_path, :class => 'btn' %>
  </div>
<% end %>
